# API base URL for production builds (use subdomain + HTTPS).
# Set when building: VITE_API_URL=https://api.pantherarestolounge.com npm run build
# Leave unset for local dev (uses http://localhost:5000).
VITE_API_URL=https://api.lasglowtech.com.ng
